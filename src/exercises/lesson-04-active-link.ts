// TODO: Return "text-teal-600 font-semibold" when pathname matches href,
// otherwise return "text-slate-500 hover:text-slate-800".
export function getLinkClass(pathname: string, href: string) {
  return "";
}
