export type PracticeImageConfig = {
  src: string;
  alt: string;
  width: number;
  height: number;
};

// TODO: Complete the image configuration with useful alt text and stable size.
export const imageConfig: PracticeImageConfig = {
  src: "/next.svg",
  alt: "",
  width: 0,
  height: 0,
};
