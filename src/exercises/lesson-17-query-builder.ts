// TODO: Build a query string from the filters object, skipping key/values where the
// value is null, undefined, or an empty string "".
export function buildQueryString(filters: Record<string, string | null | undefined>): string {
  return "";
}
