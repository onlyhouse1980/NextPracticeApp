// TODO: Read the parameter "q" from searchParams.
// Return "all" if missing or empty, otherwise return the search term string.
export function getSearchTerm(searchParams: URLSearchParams) {
  return "all";
}
