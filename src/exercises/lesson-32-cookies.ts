// TODO: Parse the cookie header (e.g. "theme=dark; session=abc") to find the value of
// the cookie matching the name argument. Return the value, or null if missing.
export function getCookieValue(cookieHeader: string | null, name: string): string | null {
  return null;
}
