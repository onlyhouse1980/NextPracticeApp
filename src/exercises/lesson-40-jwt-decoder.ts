// TODO: Decode the base64url-encoded middle payload of a mock JWT token string.
// Parse payload JSON and extract { sub, name }. Return null if any step fails.
export function decodeJwtPayload(token: string): { sub: string; name: string } | null {
  return null;
}
