export type ClassInput = string | boolean | null | undefined;

// TODO: Filter out falsy values (false, null, undefined, "") and return the remaining
// string classes joined by a space.
export function mergeClasses(...classes: ClassInput[]): string {
  return "";
}
